@model FSP.Domain.Model.Freeway
@{
    ViewBag.Title = "Create";
    
}
<div>
    @Html.ActionLink("Back to Freeways", "Index")
</div>
<h2>Create</h2>
@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)

    <fieldset>
        <legend></legend>
        <div class="editor-label">
            @Html.LabelFor(model => model.FreewayName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FreewayName)
            @Html.ValidationMessageFor(model => model.FreewayName)
        </div>        
        <div class="form-actions">
            <button type="submit" class="btn btn-primary">
                Create</button>
        </div>
    </fieldset>
}
